const nodemailerConfig = require('./nodemailerConfig')
const sessionConfig = require('./sessionConfig')
const cloudinaryConfig = require('./cloudinaryConfig')
const fileUploadConfig = require('./fileUploadConfig')

module.exports = {
    nodemailerConfig,
    sessionConfig,
    cloudinaryConfig,
    fileUploadConfig,
}
